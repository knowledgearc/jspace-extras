<?xml version="1.0" encoding="UTF-8"?>
<project name="Authentication - ORCID" default="" basedir=".">
    <property name="dest.plugins.authentication" value="${dest.plugins}/authentication"/>

    <target name="update" description="Deploy Authentication - ORCID.">
        <echo message="Deploying Authentication - ORCID to Joomla! environment."/>
        <copy todir="${dest.plugins.authentication}/">
            <fileset dir="${src}" defaultexcludes="true">
                <include name="**"/>
                <exclude name="**/language/**"/>
                <exclude name="**/build*.xml"/>
            </fileset>
        </copy>
        <copy todir="${dest.administrator.languages}">
            <fileset dir="${src}/language" defaultexcludes="true">
                <include name="**/**"/>
            </fileset>
        </copy>
    </target>

    <target name="package" description="Package Authentication - ORCID.">
        <echo message="Packaging Authentication - ORCID."/>
        <zip destfile="${package.dest}/plg_authentication_orcid.zip"
               basedir="${src}"
               update="true"
               excludes="build*.xml"/>
    </target>
</project>